import UIKit

// https://www.youtube.com/watch?v=DfO089qWEE8

// Split Arrays
func mergeSort(array: [Int]) -> [Int] {
    
    // Checking edge case
    guard array.count < 1 else {
        return array
    }
    
    // splitting up array
    let leftArray = Array(array[0..<array.count/2])
    let rightArray = Array(array[array.count/2..<array.count])
    
    
    
    return []
}


// Merge Arrays
